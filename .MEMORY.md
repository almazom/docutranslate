# .MEMORY — DocuTranslate Project Context

## Project
- **Repo:** `almazom/docutranslate` (GitHub, was `xunbu/docutranslate` — lost write access)
- **Server:** `107.174.231.22` (RackNerd VPS, user `pets`, Ubuntu)
- **Stack:** FastAPI + Vue.js SPA + Gunicorn + Nginx + Docker Compose

## Live Deployment
- **HTTPS:** `https://107.174.231.22:18443/` (self-signed SSL, CN=107.174.231.22)
- **HTTP:** `http://107.174.231.22:18080/` → 308 → HTTPS
- **Health:** `/health` — no auth, returns `{"status":"ok","version":"1.7.1.post1"}`
- **Auth:** Basic auth, user `admin`, password `tataronrails78`
- **Auth bypass:** `/health` and `/.well-known/acme-challenge/` — no auth required
- **UI defaults:** DeepSeek (`api.deepseek.com/v1`, model `deepseek-chat`), target language Russian

## Known Issues
- **IP blocked in Russian geolocation** — direct IP access doesn't work from RF
- **Solution planned:** Buy Russian domain → point to Cloudflare (free tier) → proxy to this IP
- **`git push` to `xunbu/docutranslate`** — blocked (almazom has no write access). Forked to `almazom/docutranslate`.

## Credentials on Server
- Nginx basic auth: `nginx/auth/.htpasswd` (apr1 hash)
- `.env` file at project root (ports, SSL CN, DeepSeek not configured there yet — only UI defaults)

## Tech Details
- Docker Compose: `app` (port 8010) + `nginx` (80→18080, 443→18443) + `certbot` (profile: tools)
- E2E: Playwright BDD (8 tests, `bash scripts/e2e_verify.sh`)
- Self-signed cert via `scripts/setup_ssl.sh --self-signed`
- Let's Encrypt ready: `scripts/setup_ssl.sh --domain <domain> --email <email>`
- Other services on this VPS: webssh2, ttyd, guacamole, epub-bot, phoenix-translation
