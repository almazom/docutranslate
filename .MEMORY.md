# .MEMORY — DocuTranslate Project Context

## Project
- **Repo:** `almazom/docutranslate` (GitHub, was `xunbu/docutranslate` — lost write access)
- **Server:** `107.174.231.22` (RackNerd VPS, user `pets`, Ubuntu)
- **Stack:** FastAPI + Vue.js SPA + Gunicorn + Nginx + Docker Compose

## Live Deployment
- **By IP:** `https://107.174.231.22:2053/` (self-signed SSL)
- **By domain:** `https://docutranslate.ru` (Cloudflare proxied, landing smoke green on `2026-03-30`)
- **Health:** `/health` — no auth, returns `{"status":"ok","version":"1.7.1.post1"}`
- **Auth:** Basic auth, user `admin`, password `tataronrails78`
- **Auth bypass:** `/health` and `/.well-known/acme-challenge/` — no auth required
- **UI defaults:** DeepSeek (`api.deepseek.com/v1`, model `deepseek-chat`), target language Russian

## Cloudflare Setup
- Domain: `docutranslate.ru` (Free plan)
- NS: `archer.ns.cloudflare.com` / `carlane.ns.cloudflare.com`
- DNS A: Proxied (orange cloud ☁️)
- SSL/TLS: Full
- Origin Rule: all requests → port 2053
- Registrar: nameself.com, paid until 28.03.2027

## RU Same-Domain Steering Decision
- Date: `2026-03-30`
- Chosen mechanism: proxied `Cloudflare` Load Balancer with geo steering on `docutranslate.ru`
- Decision point: `Cloudflare` edge on the proxied request, not resolver-level GeoDNS
- RU path: `docutranslate.ru` -> `Cloudflare` geo steering -> `212.28.182.235` -> reverse proxy -> `107.174.231.22:2053`
- Default path: `docutranslate.ru` -> `Cloudflare` geo steering -> current path -> `107.174.231.22:2053`
- GeoDNS was compared but rejected as the primary plan because resolver geography, ECS, and DNS cache make same-domain steering and rollback less predictable
- Keep `ru.` subdomain out of the user-facing design; only one public hostname remains

## RU Package Status
- Execution package `generated/ru-entry-for-docutranslate` is complete: cards `0001-0008` are `done`
- `RU_ACCESS_PLAN.md` now covers host capability checks, reverse proxy config, TLS, same-domain cutover, route validation, main-domain smoke/confidence, and rollback
- Playwright landing tooling now supports route-labeled proxy/default/rollback runs through `E2E_ROUTE_LABEL` and `E2E_PROXY_*`
- Hosted validation completed on `2026-03-30`: landing BDD smoke passed, direct confidence passed, proxy-mediated RU-like confidence passed, rollback-labeled confidence passed
- Cloudflare Load Balancer cutover itself is still an operator step outside the repo; the repo now contains the runbook and validation tooling for it

## RU Proxy Host
- **Deployed:** 2026-03-30
- **Host:** 212.28.182.235 (Contabo, user almaz)
- **Container:** docutranslate-ru-proxy (nginx:1.29-alpine)
- **Port:** 443 → upstream 107.174.231.22:2053
- **TLS:** Self-signed CN=docutranslate.ru (valid 1 year)
- **Status:** ✅ Running, health check OK
- **SSH key:** `/home/pets/.ssh/docutranslate_ru_proxy`

## RU Steering Status
- **RU proxy:** ✅ Deployed and tested
- **Cloudflare LB:** ⏳ Pending manual configuration
- **Geo steering:** ⏳ Not enabled yet

## Credentials on Server
- Nginx basic auth: `nginx/auth/.htpasswd` (user: admin)
- `.env` file at project root

## Tech Details
- Docker Compose: `app` (port 8010) + `nginx` (80→18080, 443→2053) + `certbot` (profile: tools)
- E2E: Playwright BDD (8 tests, `bash scripts/e2e_verify.sh`)
- Self-signed cert via `scripts/setup_ssl.sh --self-signed`
- Other services on this VPS: webssh2, ttyd, guacamole, epub-bot, phoenix-translation

## .MEMORY folder
- `.MEMORY/` — short memory cards (Momento-style) for task context
- `.MEMORY/Cloudflare/` — Cloudflare setup step-by-step cards
- Read `.MEMORY/` first when continuing multi-session work
